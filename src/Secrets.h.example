#pragma once

/*
 * Copy this file to Secrets.h and fill in your real values.
 * Secrets.h is git-ignored.
 */

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

#define TELEGRAM_BOT_TOKEN "000000000:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
#define TELEGRAM_CHAT_ID "000000000"

#define OTA_PASSWORD "change-me"

// Optional: pin Telegram's TLS root CA to verify the certificate (recommended).
// If left undefined, the connection works but the cert is NOT verified.
// Paste the PEM root CA for api.telegram.org between the raw-string delimiters.
// #define TELEGRAM_ROOT_CA R"EOF(
// -----BEGIN CERTIFICATE-----
// ...
// -----END CERTIFICATE-----
// )EOF"
